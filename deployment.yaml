apiVersion: apps/v1
kind: Deployment
metadata:
  name: staticwebsite
  labels:
    app: staticwebsite
spec:
  replicas: 1
  selector:
    app: staticwebsite
  template:
    metadata: 
      name: staticwebsite
      lables:
        app: staticwebsite
    spec:
      container:
        -name: staticwebsite
         image= sheetalsshetty/finallabexam:v1
         port= 80
    
